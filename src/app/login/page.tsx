import { PageLayout } from "@/components";
import Login from "@/components/login/Login";
const page = () => {
  return (
    <PageLayout>
      <Login />;
    </PageLayout>
  );
};

export default page;
